services:
  - type: web
    name: render-piper-tts
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt && python download_model.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT